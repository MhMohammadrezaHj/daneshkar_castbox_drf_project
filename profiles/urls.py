from rest_framework_nested import routers

from . import views

# router = routers.DefaultRouter()
# router.register("channels", views.ChannelsApiView, basename="channel")

# urlpatterns = router.urls
